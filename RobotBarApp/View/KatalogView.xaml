<UserControl x:Class="RobotBarApp.View.KatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:view="clr-namespace:RobotBarApp.View"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="1000"
             Background="#FFE5E5E5">

    <UserControl.Resources>

        <!-- Filter toggle button style -->
        <Style x:Key="FilterToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Padding" Value="25,8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#FFB0B0B0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF888888"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border CornerRadius="15"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#FF5C87B2"/>
                    <Setter Property="BorderBrush" Value="#FF3C6182"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Search TextBox style -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#FF555555"/>
        </Style>

        <!-- Card template -->
        <DataTemplate x:Key="KatalogItemTemplate">
            <view:KatalogItemView/>
        </DataTemplate>

    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top: search + filters -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Search -->
            <Border Grid.Column="0"
                    Width="330"
                    Height="45"
                    CornerRadius="20"
                    Background="White"
                    BorderBrush="#FFB0B0B0"
                    BorderThickness="1">
                <Grid Margin="12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               VerticalAlignment="Center"
                               FontSize="18"
                               Foreground="#FF808080"
                               Text="🔍"/>

                    <TextBox Grid.Column="2"
                             Style="{StaticResource SearchTextBoxStyle}"
                             Text="{Binding SearchText,
                                            Mode=TwoWay,
                                            UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </Border>

            <!-- Filters -->
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">

                <RadioButton Content="Ingredients"
                             Style="{StaticResource FilterToggleButtonStyle}"
                             GroupName="CatalogFilter"
                             IsChecked="{Binding IsIngredientsSelected, Mode=TwoWay}"
                             Command="{Binding FilterCommand}"
                             CommandParameter="Ingredients"/>

                <RadioButton Content="Drinks"
                             Style="{StaticResource FilterToggleButtonStyle}"
                             GroupName="CatalogFilter"
                             IsChecked="{Binding IsDrinksSelected, Mode=TwoWay}"
                             Command="{Binding FilterCommand}"
                             CommandParameter="Drinks"/>

                <RadioButton Content="Events"
                             Style="{StaticResource FilterToggleButtonStyle}"
                             GroupName="CatalogFilter"
                             IsChecked="{Binding IsEventsSelected, Mode=TwoWay}"
                             Command="{Binding FilterCommand}"
                             CommandParameter="Events"/>
            </StackPanel>
        </Grid>

        <!-- Items -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding CatalogItems}"
                          ItemTemplate="{StaticResource KatalogItemTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"
                                   Orientation="Horizontal"
                                   Margin="10"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</UserControl>
