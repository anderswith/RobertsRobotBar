<Window x:Class="RobotBarApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Robot Bar Dashboard" Height="500" Width="900"
        Background="#1E1E2E"
        WindowStartupLocation="CenterScreen">

    <!-- Styles -->
    <Window.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#FFD966"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Background="#2B2B3C" CornerRadius="10" Padding="15" Margin="0,0,0,10">
            <DockPanel>
                <TextBlock Text="🤖 Robot Bar Dashboard"
                           FontSize="22"
                           Foreground="White"
                           FontWeight="Bold"
                           DockPanel.Dock="Left"/>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Drink Control Panel -->
            <Border Grid.Column="0" Background="#2B2B3C" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Drink Menu" FontSize="18" Foreground="#FFD966" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ListBox x:Name="DrinkList" Background="#1E1E2E" Foreground="White" BorderThickness="0" Height="250">
                        <ListBoxItem Content="🍹 Mojito" />
                        <ListBoxItem Content="🍸 Martini" />
                        <ListBoxItem Content="🥃 Old Fashioned" />
                        <ListBoxItem Content="🍺 Beer" />
                        <ListBoxItem Content="🧉 Whiskey Sour" />
                    </ListBox>

                    <Button Content="Mix Drink"
                            Style="{StaticResource RoundedButton}"
                            Margin="0,20,0,0"
                            Click="MixDrink_Click"/>
                </StackPanel>
            </Border>

            <!-- Status Panel -->
            <Border Grid.Column="1" Background="#2B2B3C" CornerRadius="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="Robot Status" FontSize="18" Foreground="#FFD966" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="✅ Connected" Foreground="LightGreen" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="🧠 AI Mode: Enabled" Foreground="LightBlue" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="🍸 Last Drink Served: Mojito" Foreground="White" FontSize="16" Margin="0,0,0,20"/>

                    <ProgressBar Value="70" Height="20" Foreground="#FFD966" Background="#444"/>
                    <TextBlock Text="Drink Progress" Foreground="White" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
