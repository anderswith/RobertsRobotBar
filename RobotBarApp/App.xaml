<Application x:Class="RobotBarApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:RobotBarApp.Converters"
             xmlns:services="clr-namespace:RobotBarApp.Services">

    <Application.Resources>
        <conv:PathToImageConverter x:Key="PathToImageConverter"/>
        <!-- =====================
             BASIC GLOBAL BRUSHES
             (matches your app's colors)
             ===================== -->
        <SolidColorBrush x:Key="TopBarBrush" Color="#4E7A95"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3A6A81"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="LightGrayBrush" Color="#FFE5E5E5"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#DDDDDD"/>

        <!-- =====================
             CONVERTERS
             ===================== -->
        <conv:StepToVisibilityConverter x:Key="StepToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- =====================
             BASIC GLOBAL STYLES
             ===================== -->

        <!-- White rounded button -->
        <Style x:Key="RoundButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Accent-button (used for "Gem") -->
        <Style x:Key="SaveButton" TargetType="Button" BasedOn="{StaticResource RoundButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

    </Application.Resources>
</Application>
